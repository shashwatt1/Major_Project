def transcribe(audio_path: str) -> str:
    """
    Transcribe audio file to text.
    Use Whisper or SpeechRecognition.
    """
    # load model if required, transcribe and return text
    return "hello world"
