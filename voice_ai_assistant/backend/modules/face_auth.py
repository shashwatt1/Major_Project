def enroll(image_path: str, user_id: str) -> bool:
    # save sample for user_id
    return True

def verify(image_path: str) -> (bool, str):
    # return (is_authentic, user_id)
    return True, "user1"
